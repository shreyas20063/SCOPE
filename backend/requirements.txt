fastapi==0.109.0
uvicorn[standard]==0.27.0
# Explicit WS protocol dep: uvicorn[standard] bundles it, but a bare-uvicorn
# upgrade (e.g. `pip install -U uvicorn`) silently drops WebSocket support,
# breaking the /api/simulations/{id}/ws endpoint. Pin it independently.
websockets>=12.0
gunicorn==21.2.0
python-multipart==0.0.6
pydantic>=2.5.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<2.0.0
Pillow>=10.0.0
sympy>=1.12
